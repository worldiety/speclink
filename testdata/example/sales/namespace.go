package sales

// Namespace is the ReBAC resource type of the quotation domain.
//
// A constant, which the previous ForVar could not bind at all: taking the
// address of a constant is a compile error.
const Namespace = "erp.sales.quote"
